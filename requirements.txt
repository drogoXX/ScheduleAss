# Runtime dependencies, pinned with a compatible-release bound so production
# builds are reproducible. Verified against Python 3.13.
#
# Removed from earlier revisions because nothing imports them:
#   pocketbase, streamlit-authenticator (never used; not installed)
#   altair, matplotlib, regex, python-dateutil (transitive or unused)

# Core Framework
streamlit~=1.44
python-dotenv~=1.2

# Data Processing
pandas~=2.2
numpy~=2.2
openpyxl~=3.1

# Visualization
plotly~=6.0

# Report Generation
python-docx~=1.2

# Static chart export for the DOCX report (plotly fig -> PNG). Optional at
# runtime: src/reports/docx_charts.py omits figures rather than failing if the
# export backend is unavailable.
kaleido~=0.2.1
